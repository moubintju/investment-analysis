# 📚 投资业绩分析系统 - 完整指南

## 🎯 项目概述

这是一个功能完整的投资业绩分析系统，支持：
- ✅ 本地运行（Flask）
- ✅ 部署到 Vercel（Serverless）

## 📁 项目文件结构

```
investment evaluation/
├── 📂 api/                          # Vercel API 函数
│   ├── index.py                     # API 主入口
│   └── analyzer.py                  # 分析引擎（无需文件）
│
├── 📂 public/                       # Vercel 静态文件
│   └── index.html                   # 前端页面
│
├── 📂 templates/                    # Flask 模板
│   └── index.html                   # 前端页面
│
├── 📄 app.py                        # Flask 本地应用
├── 📄 Investment_evaluation.py      # 原始分析脚本
├── 📄 create_sample_data.py         # 示例数据生成器
│
├── 📄 vercel.json                   # Vercel 配置
├── 📄 requirements.txt              # Python 依赖
├── 📄 .gitignore                    # Git 忽略文件
│
├── 📖 README.md                     # 本地运行指南
├── 📖 README_VERCEL.md              # Vercel 部署指南
└── 🚀 deploy.sh                     # 部署脚本
```

---

## 🖥️ 方式一：本地运行

### 安装依赖
```bash
pip install -r requirements.txt
```

### 启动服务
```bash
python app.py
```

### 访问应用
打开浏览器访问：http://localhost:5000

### 特点
- ✅ 完整功能（包括 Excel 导出）
- ✅ 可使用真实 Excel 数据文件
- ✅ 适合开发和测试

---

## ☁️ 方式二：部署到 Vercel

### 前置条件
1. GitHub 账号
2. Vercel 账号（使用 GitHub 登录）

### 快速部署步骤

#### 1️⃣ 初始化 Git 并推送到 GitHub

```bash
# 进入项目目录
cd "c:\Users\moubin\Desktop\investment evaluation"

# 初始化 Git（如果未初始化）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Investment Analysis System"

# 在 GitHub 创建仓库后，关联并推送
git remote add origin https://github.com/你的用户名/investment-analysis.git
git branch -M main
git push -u origin main
```

#### 2️⃣ 在 Vercel 上部署

**选项 A：通过 Web 界面**
1. 访问 https://vercel.com
2. 使用 GitHub 登录
3. 点击 "New Project"
4. 从列表选择你的 GitHub 仓库
5. 点击 "Deploy"
6. 等待 1-2 分钟完成部署
7. 获取你的应用 URL

**选项 B：通过 CLI**
```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
vercel --prod
```

### 特点
- ✅ 免费托管
- ✅ 自动 HTTPS
- ✅ 全球 CDN
- ✅ 无需服务器维护
- ⚠️ 不支持 Excel 导出（已移除）
- ⚠️ 使用示例数据（需自定义真实数据）

---

## 🔄 如何使用真实数据

### 本地版本
直接将包含以下结构的 Excel 文件放在项目目录：

**Excel 要求：**
- Sheet 名称：`单元资产2025`
- 必需列：
  - `统计日期`：日期类型
  - `单元资产净值(净价)`：数字类型

### Vercel 版本
修改 `api/analyzer.py` 中的 `generate_sample_data()` 函数：

```python
def load_real_data():
    """从外部源加载真实数据"""
    # 方式 1: 从 API 获取
    import requests
    response = requests.get('https://your-api.com/data')
    data = response.json()
    return pd.DataFrame(data)

    # 方式 2: 从数据库获取
    # import psycopg2
    # conn = psycopg2.connect(DATABASE_URL)
    # return pd.read_sql("SELECT * FROM investment_data", conn)
```

然后在 `api/index.py` 中使用：
```python
sample_data = load_real_data()  # 替换 generate_sample_data()
```

---

## 📊 API 端点

### 本地版本
- `GET /` - 主页
- `GET /api/data` - 净值数据
- `GET /api/metrics` - 业绩指标
- `GET /api/summary` - 概览信息
- `GET /api/export/excel` - 导出 Excel

### Vercel 版本
- `GET /` - 主页
- `GET /api/data` - 净值数据
- `GET /api/metrics` - 业绩指标
- `GET /api/summary` - 概览信息

---

## 🔧 配置参数

在分析器初始化时可修改：

```python
analyzer = InvestmentPerformanceAnalyzer(
    data_file="your_file.xlsx",    # 数据文件
    risk_free_rate=0.015,          # 无风险利率 1.5%
    chart_title="投资业绩分析"      # 图表标题
)
```

---

## 📈 业绩指标说明

| 指标 | 说明 |
|------|------|
| **总收益率** | 从起始到当前的累计收益 |
| **年化收益率** | 转换为年度的收益率 |
| **年化波动率** | 收益率的标准差（风险度量） |
| **夏普比率** | 风险调整后收益（越高越好） |
| **最大回撤** | 历史最大跌幅 |
| **卡玛比率** | 年化收益 / 最大回撤 |

---

## 🐛 常见问题

### Q: 部署后页面空白？
A: 检查浏览器控制台，查看是否有 API 调用错误。

### Q: API 返回 500 错误？
A: 在 Vercel Dashboard 查看函数日志，确认数据格式正确。

### Q: 如何更新部署？
A:
```bash
git add .
git commit -m "更新说明"
git push
```
Vercel 会自动重新部署。

### Q: 可以使用自定义域名吗？
A: 可以，在 Vercel 项目设置中配置。

---

## 📞 支持

- 📖 查看 [README_VERCEL.md](README_VERCEL.md) 了解详细部署说明
- 📖 查看 [README.md](README.md) 了解本地运行说明
- 🐛 问题反馈：创建 GitHub Issue

---

## 📝 更新日志

### v1.0.0 (2024)
- ✅ 基础投资分析功能
- ✅ Flask 本地应用
- ✅ Vercel 部署支持
- ✅ 响应式前端界面
- ✅ 多时间段指标分析

---

**祝使用愉快！** 🎉
